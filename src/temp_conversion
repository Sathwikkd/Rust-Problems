fn fahrenheit_to_celsius(val: f64) -> f64 {
    let celsius = (val - 32.0) * (5.0 / 9.0) as f64;
    return celsius;
}
fn celsius_to_fahrenheit(val: f64) -> f64 {
    let fahrenheit = val * (9.0 / 5.0) + 32 as f64;
    return fahrenheit;
}
fn main() {
    let res1 = fahrenheit_to_celsius(200.0);
    println!("{res1}");
    let res2 = celsius_to_fahrenheit(93.3333);
    println!("{res2}");
}
